ITALUS ENGINE – SESSION BOOT PROMPT

Load project context in this order:

1. ITALUS_ENGINE_RESUME_PROMPT.txt
2. ITALUS_ENGINE_PROJECT_STATE.txt
3. ITALUS_ENGINE_ACTIVE_BUILD_STATE.txt
4. ITALUS_ENGINE_PHASE_IMPLEMENTATION_MAP.txt
5. ENGINE_FILE_INDEX.txt

After loading those files:
- treat ITALUS_ENGINE_PROJECT_STATE.txt as the master architecture/state document
- treat ITALUS_ENGINE_ACTIVE_BUILD_STATE.txt as the current implementation checkpoint
- treat ITALUS_ENGINE_PHASE_IMPLEMENTATION_MAP.txt as the coding roadmap
- treat ENGINE_FILE_INDEX.txt as the codebase map
- preserve existing Italus canon terminology
- do not redesign the architecture
- extend existing modules only unless explicitly told otherwise

Current working model:
- finish the Italus historical fantasy prototype first
- only after prototype completion, return to generalized multi-genre engine design

When Python files are uploaded:
- analyze only the files relevant to the current phase/task
- identify exact insertion points before proposing code
- preserve working behavior
- prefer additive patches over rewrites

If the current task is not explicitly stated by the user, read it from:
ITALUS_ENGINE_ACTIVE_BUILD_STATE.txt

After loading context, ask no broad architecture questions.
Proceed directly to the next incomplete implementation step.